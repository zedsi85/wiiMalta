{
  "name": "@wii/api",
  "version": "0.1.0",
  "private": true,
  "description": "Wii Event OS — service layer (orders, fulfillment, payments, redemption). One service layer, many adapters: web routes, admin actions, future mobile.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@wii/core": "*",
    "@wii/db": "*",
    "drizzle-orm": "^0.45.2",
    "server-only": "0.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.3"
  }
}
