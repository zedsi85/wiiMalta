/**
 * MOVED — the live schema now lives at packages/db/src/schema.ts (Phase 0
 * graduated this design doc into the real package). This pointer stays so the
 * companion docs' links keep working:
 *   - state-machines.md
 *   - checkout-flow.md
 * Design rationale lives in those documents; the schema itself is code now.
 */
export {};
